name: 'DF'
on:
  workflow_dispatch:
jobs:
  DF:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/galacticusorg/buildenv:latest
      options: --privileged
    steps:
      - name: Profile
        run: df -h
      - name: AutoRemove
        run: |
          apt autoremove
          apt list --installed
      - name: Measure
        run: |
          du -sh /*
      - name: Profile
        run: df -h
